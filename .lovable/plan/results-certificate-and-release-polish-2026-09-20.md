# Results, certificate, and release polish

## Scope
- Preserve the database schema, scoring formula, server-side report generation, and all final copy on Home, How Scoring Works, About, and Privacy Policy.
- Change only the scan results presentation, assurance report/certificate presentation, non-functional branding text, and release verification.

## Results screen
- Make the animated 300px sovereignty gauge the dominant result, with the existing sage, amber, and brick score states.
- Keep the category breakdown below it as smaller horizontal bars for the five scored categories.
- Keep tool findings compact: tool name, small risk pill, score, jurisdiction, and named EU alternative.
- Add a calm transition into report generation without changing when or how the live report request runs.

## Report and certificate
- Present the generated report as a clearly separated written memo with a serif heading, constrained reading width, and generous spacing.
- Refine the existing certificate document styling while keeping all fields bound to the latest session result.
- Retain the working browser print-to-PDF action and clearly explain the Save as PDF step.

## Consistency and release
- Verify both pre-scan and no-certificate empty states.
- Remove non-functional platform mentions from README/comments while preserving required package names, runtime integration identifiers, and cloud/GitHub connections.
- Run the full scan → live report → certificate flow on desktop and mobile, check current build/security status, publish, and verify the public URL.
- Confirm the managed GitHub `main` branch contains the latest saved commit; report any platform limitation explicitly.
